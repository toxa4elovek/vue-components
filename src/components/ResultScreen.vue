<template>
    <div class="alert alert-primary">
        <h3>Success: {{stats.success}} </h3>
        <h3>Errors: {{stats.error}} </h3>
        <hr>
        <button class="btn btn-primary" v-if="level > 0" @click="$emit('prevLevel')">Prev level</button>
        <button class="btn btn-primary" @click="$emit('repeat')">Repeat</button>
        <button class="btn btn-primary" v-if="level < levelCount" @click="$emit('nextLevel')">Next Level</button>

    </div>

</template>

<script>
    export default {
        props: ['stats', 'level', 'levelCount']
    }

</script>

<style>


</style>